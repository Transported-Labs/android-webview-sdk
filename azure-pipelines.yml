trigger:
- pipeline_dubug

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    ./gradlew assemble
    ls -lFht ./build/**
    ls -lFht ./lib/build/**
    ls -lFht ./build/kotlin/**
    ls -lFht ./lib/build/outputs/**
  displayName: Build aar